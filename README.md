# mnist-digit-classifier-cnn
A Convolutional Neural Network (CNN) model built using TensorFlow/Keras to classify handwritten digits from the MNIST dataset with data augmentation and optimized training techniques.
